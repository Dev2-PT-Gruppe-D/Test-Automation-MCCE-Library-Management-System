{
  "name": "library-management-api",
  "version": "1.0.0",
  "description": "Library Management System — FHB Test Automation Course System Under Test",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "seed": "node seed.js",
    "test": "npm run test:unit && npm run test:api:served && npm run test:integration:served && npm run test:e2e",
    "test:unit": "vitest run tests/unit --outputFile.junit=test-results/unit-junit.xml",
    "test:unit:watch": "vitest tests/unit",
    "test:api": "vitest run tests/api --outputFile.junit=test-results/api-junit.xml",
    "test:api:watch": "vitest tests/api",
    "test:api:served": "start-server-and-test start http://localhost:3000/api-docs.json \"npm run test:api\"",
    "test:integration": "vitest run tests/integration --outputFile.junit=test-results/integration-junit.xml",
    "test:integration:served": "start-server-and-test start http://localhost:3000/api-docs.json \"npm run test:integration\"",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "dependencies": {
    "express": "^4.18.3",
    "sql.js": "^1.12.0",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "start-server-and-test": "^2.0.8",
    "vitest": "^2.1.4"
  }
}
